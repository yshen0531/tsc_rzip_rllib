# Linux CPU cluster, Python 3.12.x
# Install:
#   pip install -r requirements.txt
#
# This file intentionally does not pin versions and does not force-install torch,
# to avoid disturbing an existing CPU-only Python environment.
# If torch is missing, install CPU torch separately:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

-i https://pypi.tuna.tsinghua.edu.cn/simple
--trusted-host pypi.tuna.tsinghua.edu.cn

--only-binary=:all:

ray[rllib]
pyarrow
gymnasium
numpy
pandas
scipy
matplotlib
tensorboard
psutil
