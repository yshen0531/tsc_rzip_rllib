# C2aA3 compact result evidence

Compact server outputs for the frozen p03 cumulative-level2 campaign. The
primary plant execution used source revision `e01411d`; the audit-only
artifact-state fix and accepted independent audit used `134b133` with zero
new plant advances. The full raw tree remains on the server and is represented
by the independently reproduced inventory digest.

The final route is a finite cumulative-level authority PASS permitting only a
separately frozen time-varying C2a design inside levels q0--2. These records
are forbidden from model or expert use.
