# C2aA2 compact result evidence

This directory contains the compact, directly copied server outputs for the
frozen `rgeo-zgeo-1ms-nr2r2c2aa2-supported-cube-directions-v1` campaign at
source revision `8bd6964`. The 5.83 GB final-raw tree remains on the server;
it is represented here by the primary and independent inventory counts,
byte total and SHA-256 digest. No raw trajectory is admitted to model or
expert use.

The primary scientific route is
`ONE_MS_NR2R2C2AA2_SUPPORTED_CUBE_DIRECTIONS_FAIL_REDESIGN`. The independent
raw audit passed and exactly reproduced all metrics. Scientific FAIL and
audit PASS are deliberately separate.
