# Stage4.2R3c3T13S24D1R14R8R27 point-versus-reserve authority discriminator design

Frozen prospectively on 2026-08-09 Asia/Shanghai after final R8R26 evidence,
compact audit, forensic report, route, and checkpoint `71bea5c` were sealed,
and before any R8R27 point-only search, plan, formal metric, classification,
or route was computed.

## 1. Scientific question and activation

R8R27 activates only for the exact final R8R26 route:

```text
ACTION_TRANSITION_SUPPORTED_MULTIRESOLUTION_AUTHORITY_INSUFFICIENT_CONTROLLER_SENTINEL_NOT_AUTHORIZED
```

Required R8R26 fingerprints are:

```text
primary detailed  a5f93c0475fd62a9f9f5cfdb542724cfd174c56e2db4e237a8c04858eab111a0
primary summary   6b4670380f72fb14e48ea47165faab02311ec363c00aff221d06c0a90e12c871
independent       2e1144940d9f3463c5d7e732e6b85ad0554d0ca24b30521c512460cfe11295ae
final report      78e1bf33e13b56c032356e7fbc57e12737b7620db158b896e82c9979ff4730d2
model evidence    532f4257871e3dd97832416e4a9be8fc447a0a90bc2949b11e1a2bfef1660307
stage manifest    0d61042e9e97c1868231a1eb7b405e7b5928d146d8a69e82b66cdbd9abfae3df
stage state       a89ab66e075d3e985f4cf0c8e067e1820fefbf301c38cf89afd42e85d61ecce6
compact audit     1a5ab3e05f841ff55af75314ec89d2e4a033a574381d1b0047f1b4b3322b0cb4
```

R8R26 proved that the final schedule reserve passes and does not enlarge the
immutable pair tube, while the final supported finite search repairs no
failed baseline. R8R27 asks a narrower diagnostic question: does the same
frozen point predictor and finite action/timing search contain a formal
repair before uncertainty reserve is added?

The answer separates two development routes:

1. point-only repair exists but reserved repair does not: uncertainty and
   excitation are the immediate limiting layer; or
2. point-only repair does not exist: the current point model's supported
   action/timing/controller family is insufficient and must be broadened.

This discriminator makes no controller, real-MPC, global-optimality,
plant-reachability, or Gate A claim.

## 2. Immutable evidence and zero-TSC boundary

Authenticate without modification or rerun:

1. all seven official R8R26 files and its compact audit;
2. R8R26's exact R8R25 and transitive source authentication;
3. the exact 432-trajectory, 27-schedule, 1,728-origin development bank;
4. the exact R8R25 all-eight planning model;
5. R8R26 pair, schedule, and combined tube arrays;
6. R8R26 interval action-transition hulls, causal state support, hard action
   construction, baseline scoring table, and formal evaluator; and
7. the exact final result that the combined-reserve search completed `16/16`
   and produced zero robust-formal plan.

R8R27 executes zero Ray, `gotsc`, TSC, controller, plant advance, snapshot,
or raw creation. It does not modify any source stage. Every R8-family
trajectory remains forbidden from expert, BC, DAgger, residual-RL, or any
other policy-learning data.

## 3. Frozen three-layer comparison

Use exactly three uncertainty layers in this order:

```text
point_only     zero tube with the exact four interval shapes
pair_tube      immutable R8R25 all-eight outer-jackknife planning tube
combined_tube  final R8R26 componentwise max(pair_tube, schedule_tube)
```

The source evidence already states that `pair_tube` and `combined_tube` have
the same digest. R8R27 must authenticate their elementwise equality before
opening `point_only` results, and must reproduce the final R8R26
`combined_tube` plans, search counters, token digests, predictions, formal
metrics, and route within absolute tolerance `1e-12`.

No tube may be rescaled, clipped, quantiled, refit, localized, selected by
outcome, or modified after inspecting any layer. The point predictor is not
refit or adapted. No innovation, future observation, pair/history label,
source outcome, or heldout formal result enters planning.

## 4. Search, support, and action contract remain bit-for-bit fixed

For every layer and all 16 contexts, retain R8R26 exactly:

```text
decision task steps              [10,14,18,22]
candidate lattice                u,v integers; u>=0; v>=0; u+v<=24
coordinate                       q=(u/16,v/16)
coarse candidate count           33
beam width                        256
terminal seeds                    32
refinement step units             [2,1]
maximum sweeps per step           16
```

Every node must pass the unchanged causal visible-state support,
interval-specific affine/convex action-transition support, exact Card15
construction, incremental and total action limits, current utilization,
desired/applied cosine, off-basis residual, saturation, clipping, finite, and
stop-before-advance rules.

The partial and terminal ranking tuples, direction order, tie-breaking,
baseline fallback, and prohibition on deploying a best failing plan remain
unchanged. R8R27 is not a new optimizer search or hyperparameter comparison;
only the frozen tube input differs between layers.

## 5. Formal contract and postselection scoring

Formal timing remains immutable:

```text
slew 1.0/1.1: arrive by 250 ms; hold through 350 ms
slew 0.9:     arrive by 270 ms; hold through 370 ms
R/Z: 30 mm; speed: 0.1 m/s; Ip: 10000 A; arrival streak: 3
```

For each layer, a plan is selected causally from model, tube, current visible
history, and issued-action history only. If the selected plan is not robust-
formal under that layer, the causal policy selects the unchanged baseline
continuation. Only after that choice is frozen may the immutable authentic
baseline formal label be used to count repairs, regressions, and
baseline-fallback-plus-plan oracle.

Report for each layer:

```text
safe search contexts
robust-formal plan contexts
repairs among ten failed baselines
regressions among six baseline passes
fallback-plus-plan oracle
minimum and maximum selected formal violation
all search counters and evaluated-token digests
```

## 6. Frozen integrity and classification gates

All source hashes, bank/model/tube/hull/support/action/formal/search
reproductions, finite checks, and independent agreement are all-or-nothing.
Every layer must safely complete all 16 searches. The pair and combined
layers must agree exactly, and the combined layer must reproduce R8R26
exactly.

After those integrity gates pass, classify using only point-only repairs:

```text
point-only repairs >= 1 and point-only oracle >= 7/16:
  POINT_AUTHORITY_PRESENT_RESERVED_UNCERTAINTY_EXCITATION_REDESIGN_REQUIRED

otherwise:
  POINT_ACTION_TIMING_AUTHORITY_INSUFFICIENT_BROADER_CONTROLLER_REDESIGN_REQUIRED
```

Frozen fail-closed route:

```text
source, reproduction, search, causality, finite, or independent failure:
  R8R27_INTEGRITY_FAIL_STOP
```

The classification threshold is not a relaxed formal-control gate. A
point-only formal pass is an optimistic model diagnostic with zero reserve;
it is never safe or deployable controller evidence.

## 7. Independent implementation

Primary and structurally independent implementations must separately rebuild
the source bank/model identity, support decisions, transition hulls, hard
action issues, three searches, formal metrics, postselection counts, and
route. The independent path imports the R8R26 independent audit chain, not
the R8R26 or R8R27 primary implementation.

They must agree on every discrete decision and digest exactly and on every
floating value within absolute tolerance `1e-12`. Postprocess is forbidden
unless every agreement gate passes.

## 8. Authorization boundary

Either valid classification authorizes only prospective design of its named
next development route. R8R27 cannot authorize a real controller sentinel,
claim MPC qualification or Gate A, create expert data, or enter BC, DAgger,
or residual RL. A later fresh controller remains mandatory before Gate A.
