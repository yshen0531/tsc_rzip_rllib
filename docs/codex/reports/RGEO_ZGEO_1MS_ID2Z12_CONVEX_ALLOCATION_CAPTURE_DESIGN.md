# R_geo/Z_geo 1 ms ID-2Z12 convex-allocation capture design

Date: 2026-08-20 Asia/Shanghai

## Purpose

ID-2Z12 is the final finite authority discriminator for the signed B/F
two-dimensional action basis at the exact ID-2Z11 selected state-61 history.
It replaces vertex-only/time-shared hand macros with an algorithmically
generated boundary of the exact simultaneous Card15 allocation polygon.

It is not a model, policy, recovery controller, Recourse-L1 result, waypoint
or global reachability test.

## Frozen action set

Let B be one exact `p07:minus` increment and F one exact `p03` forward
increment. A per-issue allocation is `(alpha_B, alpha_F)`. The unchanged
per-coil slew constraint produces the exact feasible diamond
`|alpha_B|+|alpha_F|<=1`.

The nine candidates are fixed in clockwise order:

```text
hold       ( 0.0, 0.0)
b4         (+1.0, 0.0)
bf4        (+0.5,+0.5)
f4         ( 0.0,+1.0)
pf4        (-0.5,+0.5)
p4         (-1.0, 0.0)
pu4        (-0.5,-0.5)
u4         ( 0.0,-1.0)
bu4        (+0.5,-0.5)
```

Each non-hold candidate repeats its exact simultaneous allocation for four
issues. Half allocations are constructed in Card15 decimal fields before
conversion to actual current; silent floating clipping is forbidden.

## Receding branch experiment

All branches replay the full authenticated ID-2Z11 selected prefix.

1. At state 61, execute all nine candidates to the common state-77 horizon.
   Select capture-first, otherwise minimum frozen terminal score; commit all
   four issues.
2. At the true selected state-65 history, execute the same nine candidates to
   state 77 and select once more.
3. Fresh reset and exactly replay the complete selected path.

Terminal states are 72--77. Capture requires every terminal state to satisfy
distance `<=25 mm`, speed `<=0.1 m/s`, and absolute source-relative Ip
fraction `<=5%`. Relative score improvement is never PASS.

The budget is 18 teacher branches plus one replay: at most 19 resets, 1,463
advances, 1,482 states and 7,410 required artifacts. No retry or extra root
is permitted.

## Safety and evidence

Current exact/noiseless same-step paired-boundary R_geo/Z_geo and Ip, plus
the full post-takeover causal action/readback history, are available before
every issue. Future successors remain unknown. Exact Card15, `<=0.3 A`
per-coil slew, absolute current, limiter, prefix, boundary, Ip, runtime,
empirical successor trip, hard envelope, raw inventory and independent raw
audit remain fail closed.

The development shell is not a recovery set. All trajectories are route and
authority evidence only and are forbidden from fitting, calibration,
holdout, controller, expert, BC, DAgger, RL or fixture use.

## Routes

PASS requires selected-path six-state capture, exact fresh replay and all
evidence gates. It authorizes only a separately designed fresh capture replay
and Recourse-L1 candidate qualification.

FAIL closes the complete signed B/F convex-allocation basis at this causal
history. The next review must move earlier in the takeover/nominal design or
change the physical action basis. It may not add a third receding round,
adjacent root, denser coefficient grid, relaxed capture set or larger model.

## Final-goal boundary

The final goal remains fixed-1100-ms, 1-ms exact-RZI, full-causal-history,
exact-Card15 two-axis waypoint/path control under independent hard safety and
recovery, eventually including bidirectional repeated R_mid crossing with
continuous belief. ID-2Z12 is only one finite source-local discriminator.
