# Stage4.2R3c3T13S24D1R14R8R28 front-loaded cumulative endpoint timing authority sentinel design

Frozen prospectively on 2026-08-09 after final R8R27 primary/independent
agreement and forensic closure, but before R8R28 implementation,
configuration, offline construction, candidate action, formal metric, raw,
snapshot, Ray, `gotsc`, TSC, controller, or plant execution.

## 1. Question and necessity

R8R27 proved that removing all prediction reserve still repairs `0/10` failed
baselines within the frozen four-slot U/V family at task steps
`[10,14,18,22]`. Its six point-feasible plans all belong to already-passing
baselines. Uncertainty rescaling is therefore not the next route.

The physical cumulative atlas nevertheless showed before R8R27 that the
direction-2-positive endpoint U produced the best measured margin gain in six
failed contexts and the direction-1-negative endpoint V did so in four. All
existing cumulative U/V campaigns place their four major issues at the same
four task steps. They do not test whether applying the same four cumulative
increments earlier provides enough time for arrival and formal hold.

R8R28 asks exactly:

```text
Does front-loading the unchanged four cumulative U or V endpoint issues,
without changing the first causal issue, issue count, direction, scale,
final cumulative current displacement, formal deadline, or safety boundary,
physically repair at least one of the ten failed finite restart contexts?
```

R8R28 is a fresh real-TSC finite timing-authority sentinel. It is not a
causal selector, feedback controller, MPC, Gate A, expert-data, imitation, or
learning stage. Its per-context held oracle is evaluator-only and forbidden
to the controller.

## 2. Immutable source contracts

Use the exact accepted R8R7 baseline source:

```text
run
  stage4_2r3c3t13s24d1r14r8r7_fresh_multipulse_static_observer_interaction_sentinel_20260807_d8d231e_v1
route
  FRESH_MULTIPULSE_STATIC_OBSERVER_INTERACTION_PASS_MPC_DESIGN_REQUIRED
baseline raw
  16 files / 487298 bytes
  46df626a462dfdbfe7cdf9138a50b6b19c03f0ae5e8bb18cf18c6fcbe05c01a5
final / manifest / state SHA-256
  9ca6afce52442c1b7470cb8ff13a2eac4aab32de1e05a898d206f5fe76e01005
  ae89fb2df01cd6758676baa895880e2005a1f8967c62ea4ae671ab61ea771e24
  04f643e09f414c5d5a305f1a3584450e12e5a39e8d062d454480df3c8d95fe7e
```

Authenticate the final R8R14 cumulative atlas and use its exact canonical
matrix, dynamic exact-Card15 construction, actuator contract, source prefix,
U/V endpoint definitions, and formal evaluator:

```text
run
  stage4_2r3c3t13s24d1r14r8r14_cumulative_multidirection_staircase_authority_identification_20260808_9874068_v1
route
  CUMULATIVE_MULTIDIRECTION_ATLAS_AUTHORITY_INSUFFICIENT_SEQUENCE_BASIS_REDESIGN_REQUIRED
U
  canonical direction 2, sign +1, scale 1.0
V
  canonical direction 1, sign -1, scale 1.0
new raw
  112 files / 3670959 bytes
final / manifest / state SHA-256
  da8420de6520e8f5fba14a9acf0489a89f7f2e1b9c2f613ebb91ef6e0b011b61
  ce980faf285b431910672cd819ea3e3b8da35d8f91507295616fda1a6df7a7f3
  27a2d6001c920aa3149c86a3bf6f52009acd42cf78bf5ac68500f6b3441b1eb3
compact evidence SHA-256
  99af9763a0a468dc6677edf24211e7b57b1c6b5a17c023d1d26a32358bf3dcfd
```

Authenticate final R8R27 as route provenance:

```text
run
  stage4_2r3c3t13s24d1r14r8r27_point_versus_reserve_authority_discriminator_20260809_0832c6b_v1
route
  POINT_ACTION_TIMING_AUTHORITY_INSUFFICIENT_BROADER_CONTROLLER_REDESIGN_REQUIRED
primary / summary / independent / final / manifest / state SHA-256
  
```

No source byte may change, and no R8, R8R1, or prior R8-family trajectory may
be rerun. Every source and R8R28 trajectory is identification/control-
development evidence only and is forbidden from expert, BC, DAgger, and RL
data.

## 3. Exact timing family

The first issue remains at causal task step 10. The only changed variable is
the timing of the second through fourth major issues. Freeze exactly two
front-loaded grids:

```text
grid_id  decision task steps  interpretation
g3       [10,13,16,19]        three-step uniform spacing
g2       [10,12,14,16]        two-step uniform spacing
```

For each grid, execute exactly two constant endpoint sequences:

```text
sequence  endpoint at all four major issues
UUUU      R8R14 direction 2, sign +1, scale 1.0
VVVV      R8R14 direction 1, sign -1, scale 1.0
```

There are no mixed U/V sequences, per-context plan choices, amplitude scan,
reserve scaling, outcome-selected schedule, or post-result timing. The family
is exactly four candidates over each of the sixteen ordered R8R7 contexts:

```text
2 grids * 2 endpoint sequences * 16 contexts = 64 trajectories
major exact Card15 issues                     = 256
stored-target refreshes                       = 1408
```

At every major issue, construct the exact same R8R14 coordinate increment
from the current causal measured-current center. Hold its exact stored Card15
target by causal refresh until the next major issue, then hold the fourth
target through the unchanged formal endpoint. For a normal context there are
21 refreshes; for a weak-slew context there are 23. The final cumulative
coordinate contains four identical endpoint increments, exactly as the
corresponding R8R14 UUUU or VVVV candidate. Timing may change the intervening
physical response but may not change action semantics.

The delegated source trajectory and causal trace must remain exact through
task step 9. Pair/history labels, source result, future state/action/current,
wire current, formal outcome, and another member are forbidden controller
inputs. Numeric target, visible R/Z/Ip and causal velocities, task clock,
past/current measured coil currents, and the fixed candidate identity are
the only allowed inputs.

## 4. Dual offline construction and hard safety

Before any worker, raw file, snapshot, or plant advance, primary and
structurally independent offline paths must authenticate all sources and
construct all 64 specs, 256 major issues, and 1,408 refreshes exactly. Every
action must pass the unchanged R8R14 gates:

```text
exact Card15 target/readback reconstruction
finite requested and applied action/current
incremental normalized action <= 0.25
total normalized action <= 1.0
maximum current utilization <= 0.55
no action saturation or current clipping
desired/applied-current cosine >= 0.98
relative off-basis residual <= 0.10
safe stop before a failed plant advance
```

Primary and independent spec identity, source snapshot, action, current,
target-chain, count, and digest results must agree exactly. Any offline
failure ends R8R28 with zero new TSC. The family may not be reduced after
seeing which grid or endpoint failed.

## 5. Prospective two-phase execution

Preserve the unchanged ordered eight physical pairs and history-member order.
The first two pairs form four safety contexts:

```text
safety         4 contexts * 4 candidates = 16 trajectories
qualification 12 contexts * 4 candidates = 48 trajectories
maximum total                         = 64 trajectories
```

Safety opens only runtime, authentic restart/source-prefix state and trace,
causality, calibration, fixed candidate identity, exact timing, exact
Card15/action/refresh, current, saturation, finite state, forbidden-input,
raw, snapshot, and aggregate count outcomes. Formal tracking outcomes remain
closed. Qualification is authorized only after complete primary/independent
safety raw agreement and immutable safety-raw authentication.

Formal evaluation opens only after all 64 raw files strictly parse and dual
qualification raw audits agree. A rejected action must not be applied and no
later plant step may occur. A runtime, safety, integrity, or scientific fail
cannot resume under changed semantics.

## 6. Immutable formal evaluation and authority gate

Use both unchanged formal paths:

```text
slew 1.0/1.1  arrive by state 25, hold through state 35
slew 0.9      arrive by state 27, hold through state 37
R/Z <= 0.03 m, speed <= 0.1 m/s, Ip <= 10000 A
arrival streak 3
metric-path absolute tolerance <= 1e-12
```

The earlier action schedule does not move an arrival deadline or shorten the
required hold. First reproduce the R8R7 baseline result exactly at `6/16` and
all saved formal metrics at `<=1e-12` difference.

Report each fixed global candidate's pass count, repairs, baseline-pass
regressions, arrival/hold metrics, signed minimum margin, current use, and
failed-context margin-gain distribution. A do-nothing-safe held oracle may
select the baseline or one of four candidates only after all raw passes. It
is evaluator-only and cannot be exposed to a controller.

The scientific gate passes only if:

```text
all source/offline/raw/safety/formal/independent integrity gates pass
failed baseline count                                      10
at least one failed baseline repaired by a real candidate  >= 1/10
held baseline-or-four-candidate oracle                      >= 7/16
all six baseline-pass contexts retained by baseline fallback
primary/independent context outcomes, numerics, gate, route exact
```

There is no global-optimality claim. A PASS establishes only finite measured
timing authority and may authorize design of a separately frozen causal
visible-state feedback controller using this timing family. It is not a
controller or Gate A.

## 7. Frozen routes

```text
offline source/action/timing construction failure before TSC
  FRONT_LOADED_ENDPOINT_TIMING_PREFLIGHT_FAIL_NO_TSC

runtime/restart/causality/Card15/current/raw/safety failure
  FRONT_LOADED_ENDPOINT_TIMING_EXECUTION_OR_SAFETY_FAIL_STOP

integrity passes but no failed baseline is repaired
  FRONT_LOADED_ENDPOINT_TIMING_AUTHORITY_INSUFFICIENT_BROADER_CAUSAL_CONTROLLER_REDESIGN_REQUIRED

at least one measured repair and held oracle >= 7/16
  FRONT_LOADED_ENDPOINT_TIMING_AUTHORITY_PASS_CAUSAL_FEEDBACK_CONTROLLER_DESIGN_REQUIRED
```

A PASS does not authorize direct deployment, expert data, BC, DAgger,
residual RL, or Gate A. A FAIL rejects only these two front-loaded grids and
two fixed cumulative endpoint sequences; it does not prove plant
unreachability or global timing insufficiency. All classifications must
separate runtime/deployment, restart/causality, raw/reporting, safety,
finite-design authority, real-controller, and plant conclusions.
