# Stage4.2R3c3T13S24D1R14R8R22 bounded continuous-multidirection authority sentinel design

Frozen prospectively on 2026-08-08 Asia/Shanghai after final R8R20 evidence,
route, and forensic report were sealed, but before R8R22 implementation,
configuration, offline construction, candidate output, raw, TSC, formal
metric, or route.

## 1. Scientific question

R8R14 found that the best scale-1.0 constant staircase for each of the ten
failed contexts was either direction-2 positive or direction-1 negative, but
repaired none. R8R15/R8R20 then measured the complete four-slot binary tree
over those two endpoints; every sequence still preserved the `6/16` formal
partition. R8R20 nevertheless found a positive best-candidate minimum-margin
gain in every failed context.

R8R22 asks one bounded physical question: can a prospectively fixed larger-
amplitude convex combination of those two measured useful directions repair
at least one failed context while preserving all hard safety contracts?

R8R22 is finite physical authority identification. It is not a fitted model,
per-context selector, online adaptive controller, real MPC, Gate A, or global
reachability test.

## 2. Immutable development evidence

Authenticate without modifying or rerunning:

1. final R8R7 baseline and authentic restart/source-prefix evidence;
2. final R8R12 direction-2-positive staircase;
3. all final R8R14 four-direction/two-sign constant staircases;
4. all final R8R15 and valid R8R20 Boolean-sequence evidence; and
5. final R8R20 primary/independent agreement and route.

R8R14/R8R15/R8R20 are development evidence used only to choose the new
closed action family. No old raw is a R8R22 held result. The failed R8R20
`_v1` session-interruption workspaces are forbidden evidence and must not be
read by R8R22.

The physical controller boundary remains the exact R8R14/R8R20 boundary:

```text
delegated authentic source prefix through task step 9
decision task steps [10, 14, 18, 22]
fresh exact Card15 target from current causal coil readback at every issue
exact stored-target refresh between issues and through formal hold
```

## 3. Closed candidate family

Let the unchanged canonical response-coordinate columns be `c0,c1,c2,c3`.
Define the two useful endpoints exactly as:

```text
U = +c2
V = -c1
```

For mixing weight `w` and amplitude `a`, use one constant cumulative
staircase coordinate at all four decisions:

```text
c(w,a) = a * (w*U + (1-w)*V)
w in [0.00, 0.25, 0.50, 0.75, 1.00]
a in [1.25, 1.50]
```

The exact ten candidates are the Cartesian product in amplitude-major,
weight-minor order. Decimal values are parsed as exact design tokens before
float conversion. No scale, weight, sign, third direction, temporal switch,
decision step, cancellation, context-specific branch, or family expansion
may be added after construction or outcome inspection.

This gives:

```text
new candidates                                      10
contexts per candidate                              16
new specifications                                 160
levels/issues per specification                      4
new issue constructions                            640
new stored-target refresh constructions          3,520
```

Existing scale-1.0 endpoints and Boolean sequences must not be rerun.

## 4. Dual offline construction gate

Before Ray, `gotsc`, controller, raw, or plant advance, primary and
structurally independent implementations must separately construct all 160
specifications from the authenticated source prefix. They must agree on
every context, candidate token, canonical coordinate, level, exact Card15
target, action, nominal readback, issue/refresh increment, utilization,
cosine, off-basis residual, and pass/fail decision.

Every construction must pass the unchanged hard gates:

```text
incremental normalized action <= 0.25
total normalized action       <= 1.00
maximum current utilization   <= 0.55
desired/applied cosine        >= 0.98
relative off-basis residual   <= 0.10
exact Card15 issue and refresh
no action saturation or current clip
finite requested/applied action and current
```

The larger amplitudes are authorized only if all 160 nominal chains pass.
Any failure or dual-construction disagreement ends R8R22 with zero new TSC:

```text
BOUNDED_CONTINUOUS_MULTIDIRECTION_PREFLIGHT_FAIL_NO_TSC
```

## 5. Prospective safety, calibration, and held qualification

Use the unchanged ordered eight physical pairs and both histories. The first
two pairs are the execution/safety calibration partition; their formal
tracking outcomes stay closed. The other six pairs are held qualification:

```text
safety/calibration  4 contexts * 10 candidates    40 trajectories
held qualification 12 contexts * 10 candidates  120 trajectories
maximum new authentic trajectories               160
```

Qualification is unauthorized until all 40 safety raw pass primary and
independent runtime, full-horizon, authentic restart/source-prefix,
calibration, exact-coordinate, target-chain, Card15, action, current,
finite-state, forbidden-input, strict-JSON, and inventory agreement.

At every physical issue and refresh, recompute all hard gates from the live
causal readback before the next plant advance. A failed action is rejected,
and no later plant step occurs. Any runtime, restart, causality, Card15,
current, saturation, raw, or dual-audit failure stops as:

```text
BOUNDED_CONTINUOUS_MULTIDIRECTION_EXECUTION_OR_SAFETY_FAIL_STOP
```

A completed trajectory may never be rerun. Resume is permitted only for
missing specifications under byte-identical package, source fingerprints,
spec matrix, controller, and action semantics.

## 6. Immutable formal authority gate

Formal evaluation opens only after all 160 new raw pass both phase audits.
Use the unchanged contract and evaluator:

```text
slew 1.0/1.1: arrive <= 250 ms, hold/evaluate through 350 ms
slew 0.9:     arrive <= 270 ms, hold/evaluate through 370 ms
R/Z tolerance 30 mm
speed threshold 0.1 m/s
Ip threshold 10000 A
arrival streak 3
primary/independent metric tolerance 1e-12
```

First reproduce the immutable baseline `6/16` and every source candidate
metric exactly. Then report each fixed global R8R22 candidate, repairs,
regressions, arrival/hold metrics, signed margins, and the evaluator-only
do-nothing-safe oracle over baseline plus the ten new candidates.

The scientific gate passes only if all are true:

```text
source/offline/raw/safety/formal/independent gates          pass
R8R7 failed baselines                                     10
R8R22 repairs among failed baselines                    >= 1/10
held baseline-plus-R8R22 oracle                         >= 7/16
oracle strictly exceeds baseline                             yes
primary/independent formal values, decisions, route          exact
```

PASS route:

```text
BOUNDED_CONTINUOUS_MULTIDIRECTION_PASS_CAUSAL_RECEDING_HORIZON_CONTROLLER_DESIGN_REQUIRED
```

Authority-FAIL route:

```text
BOUNDED_CONTINUOUS_MULTIDIRECTION_AUTHORITY_INSUFFICIENT_ONLINE_FEEDBACK_MODEL_REDESIGN_REQUIRED
```

A PASS establishes only finite physical authority for a separately frozen
causal model/selector and fresh controller holdout. A FAIL rejects only the
ten fixed constant-staircase coordinates; it does not show wrong-direction
control or global plant unreachability.

## 7. Integrity and learning boundary

Primary and independent implementations must be structurally separate and
agree on exact source hashes, construction, phase inventories, raw metrics,
formal outcomes, repairs, regressions, oracle, and route. Large raw and TSC
workspaces remain on the server; download compact hashed evidence only.

Every R8R22 and earlier R8-family trajectory is probe/controller-development
evidence and is forbidden from expert, BC, DAgger, and RL data. Gate A,
expert data, BC, DAgger, and residual RL remain blocked regardless of R8R22
outcome.
