# R_geo/Z_geo 1 ms ID-2G1R1 full-Card15 grouped model comparison design

Date: 2026-08-17 Asia/Shanghai

Identity: `rgeo-zgeo-1ms-id2g1r1-full-card15-grouped-model-comparison-v1`

ID-2G1R1 prospectively repairs only ID-2G1's false exact-rank-three executor
representation.  ID-2G1 remains final as
`ONE_MS_ID2G1_INPUT_OR_DATA_INTEGRITY_FAIL_NO_MODEL`, with zero fits and zero
TSC/plant work.

All ID-2G1 evidence, 39-unit-cell weighting, three whole-context LOCO folds,
state origins, targets, neural capacities, seeds, training budget, metrics,
thresholds, simplicity order, forbidden reads and authorization boundaries
remain unchanged.  The base ID-2G1 config is hash-bound and this R1 config is
an explicit overlay rather than a silent edit.

The sole scientific change is the action/current feature contract:

- current observed executor state is the complete 14-dimensional same-step
  coil readback relative to the exact source readback;
- issued action is the complete 14-dimensional exact Card15 target relative
  to q0;
- the predictor sees current, issued and issued-minus-current vectors without
  a learned or assumed low-dimensional projection;
- recursive future executor state is propagated from the candidate issued
  target and never read from future raw/current;
- the action-blind comparator replaces each non-nominal future issued vector
  with the exact time-matched active-nominal issued vector while retaining
  causal observed current/history;
- p03/p04/p07 virtual coordinates remain evaluator/grouping metadata only and
  never become executor truth or model input.

The structured model applies the same four stable poles independently to the
14 exact action-difference channels with ridge regularization.  GRU and TCN
input width changes mechanically to the full-Card15 feature width; hidden
width, layers, dilations, seeds, optimizer and epoch budget do not change.

Routes are:

- `ONE_MS_ID2G1R1_INPUT_OR_DATA_INTEGRITY_FAIL_NO_MODEL`;
- `ONE_MS_ID2G1R1_TRAINING_OR_REPRODUCIBILITY_FAIL_STOP`;
- `ONE_MS_ID2G1R1_GROUPED_MODEL_FAIL_ROUTE_REVIEW`;
- `ONE_MS_ID2G1R1_GROUPED_MODEL_PASS_FRESH_CALIBRATION_DESIGN_ONLY`.

A PASS still authorizes only fresh calibration design.  ID-2C2, holdout,
controller, tube, recourse, MPC, transport/crossing, adaptation and RL remain
blocked.
