# Stage4.2R3c3T13S24D1R14R8R51R4D4 center-bridged two-pulse response sentinel design

Status: prospectively frozen on 2026-08-10 after final R51R4D3 geometry
forensics, but before R51R4D4 implementation, offline event construction,
real authorization, TSC execution, raw creation, sequential response, formal
metric, model, controller, or optimization.

## 1. Scientific question and source boundary

R51R4D3 proved that the complete ordered product of five frozen transport
targets can be represented in all ten failed contexts as two individually
safe exact Card15 pulses separated by exact return to and hold at the stored
q0 center. D3 ran no plant and did not measure whether the schedule moves the
plasma usefully.

R51R4D4 asks only:

```text
Does any prospectively frozen center-bridged two-pulse schedule produce a
safe, causal, full-horizon measured TSC response that repairs at least one of
the ten failed formal contexts under the immutable timing contract?
```

D4 must hard-authenticate final accepted D3, including its 250 specs, exact
action-stream digest, primary/independent PASS, package fingerprints, source
authentication, final state, final report, and server evidence. It must also
authenticate the immutable R51R4/R51R3/R51R1/R8R7 baseline and raw inventories
inherited by D3. No old trajectory may rerun.

## 2. Frozen matrix and identities

Use every D3 cell under new D4 experiment identities:

```text
failed physical contexts = 10
candidates               = d0m, d1p, d2m, d3p, u1p50
ordered pair product      = 5 x 5
real trajectories         = 10 x 25 = 250
```

No context, history member, first candidate, second candidate, equal pair,
failed offline cell, structured stop, or real outcome may be excluded. All
250 specs are probes forbidden from expert or other learning data.

## 3. Frozen causal action schedule

Each worker starts from the authentic context snapshot with a fresh
controller wrapper and fresh TSC process. The complete clock is:

```text
source semantic prefix                                  task steps 0..9
shared exact Card15 q0 issue                            task step 10
q0 observation hold                                    task step 11
first candidate issue                                  task step 12
first candidate exact-current holds                    task steps 13..15
first exact stored-center return                       task step 16
exact zero-increment q0-center bridge hold             task step 17
second candidate issue from restored q0 center         task step 18
second candidate exact-current holds                   task steps 19..21
second exact stored-center return                      task step 22
exact q0-center refresh                                through state 35/37
```

The first physical effect is state 13 and the second physical effect is state
19. Every action is reconstructed online from only the immutable spec, visible
current, exact Card15 actuator boundary, causally stored center, and current
task clock. Future state/response, formal outcome, hidden label, pair/history
identity as a control input, another rollout, wire/vessel current, and source
result are forbidden.

## 4. Dual offline construction and authorization

Before Ray, `gotsc`, TSC, or raw creation, primary NumPy and structurally
independent scalar paths must reconstruct all 250 event streams. The
independent path may share exact actuator primitives but may not call the
primary schedule constructor, primary eligibility, or primary coverage code.

Offline PASS requires:

```text
final D3/source/hash authentication                         PASS
specifications and fixed clocks                         250/250
q0 issue normalized Linf <= 1e-5                        250/250
exact first/second identities and coordinates           250/250
exact Card15 issues, holds, bridge, returns, refresh     250/250
incremental normalized action Linf <= 0.25               250/250
total normalized action abs <= 1.0                       250/250
predicted current utilization <= 0.55                    250/250
desired/applied current cosine >= 0.98                   250/250
relative off-basis residual <= 0.10                      250/250
no clipping, saturation, forbidden input, or exclusion  250/250
context/history-pair coverage                          10/10, 5/5
primary/independent event, gate, digest agreement          exact
primary/independent numerical difference                <= 1e-10
```

The post-return componentwise-zero command representation is diagnostic only,
exactly as prospectively frozen for D3. Exact physical current/Card15 center
and every event gate remain mandatory. Any offline failure freezes D4 with no
real TSC.

Real authorization additionally requires a separately committed conditional
R51R4D5 model/controller-preflight design frozen before any D4 real response.

## 5. One authorized real campaign and safe stop

Only dual offline PASS plus the conditional-D5 design checkpoint authorizes
one fresh campaign. A complete trajectory may never rerun. At most 250 new
TSC processes, 250 raw files, and 9,050 successful plant advances are allowed.

Before every plant advance the real controller must reconstruct and recheck
the corresponding frozen event. If an action, Card15, current, nonfinite,
clipping, saturation, forbidden-input, or abnormal-state gate fails, it must
record a structured safe stop before applying the rejected action and must not
advance the plant again. A partial or stopped trajectory remains in raw and
cannot be relabeled as a measured authority result.

## 6. Raw integrity before response

Primary and structurally independent raw audits must complete before any
formal response is opened:

```text
strict JSON.GZ parse and immutable spec                    250/250
full horizon and successful execution                     250/250
authentic restart and source physical/semantic prefix      250/250
causal trace and forbidden-input audit                     250/250
q0, both issues, holds, bridge, returns, refresh exact      250/250
first/second effects at states 13/19                       250/250
finite R/Z/Ip, coil and vessel/wire state                  250/250
no runtime/solver/saturation/abnormal error                250/250
raw count, bytes, digest, and row identities                  exact
primary/independent action/state/integrity agreement          exact
```

Any failure above selects the execution/integrity route. It is not a
scientific authority failure, and formal metrics remain unopened.

## 7. Frozen formal response gate

Only after both raw-integrity audits pass may primary and structurally
independent formal evaluators consume R/Z/Ip. Apply unchanged:

```text
R/Z tolerance                         30 mm
speed threshold                       0.1 m/s
Ip thresholds                         frozen R51R4 values
arrival streak                        3 samples
slew 1.0/1.1 arrival deadline         250 ms
slew 0.9 arrival deadline             270 ms
slew 1.0/1.1 hold endpoint            350 ms
slew 0.9 hold endpoint                370 ms
```

The 16 baseline outcomes must reproduce exactly `6/16`. Every D4 row must be
reported. Scientific PASS requires both:

```text
failed baseline contexts repaired by >=1 measured D4 row       >= 1/10
baseline-plus-D4 measured oracle formal contexts                >= 7/16
```

Primary and independent discrete outcomes must agree exactly and all metric
differences must be `<= 1e-12`. No arrival deadline, tolerance, row, or
aggregate criterion may change after response.

## 8. Frozen routes

```text
D3/source/hash/conditional-design authentication fails
  REDUCED_Q0_TRANSPORT_BRIDGE_R51R4D4_BLOCKED_BY_SOURCE_OR_DESIGN

dual offline construction fails
  REDUCED_Q0_TRANSPORT_BRIDGE_R51R4D4_CENTER_BRIDGED_TWO_PULSE_OFFLINE_FAIL_NO_REAL_TSC

runtime, safe-stop, restart, raw, causality, action, or independent integrity fails
  REDUCED_Q0_TRANSPORT_BRIDGE_R51R4D4_CENTER_BRIDGED_TWO_PULSE_EXECUTION_FAIL_STOP

integrity passes but repair/oracle gate fails
  REDUCED_Q0_TRANSPORT_BRIDGE_R51R4D4_CENTER_BRIDGED_TWO_PULSE_AUTHORITY_INSUFFICIENT_CAUSAL_FEEDBACK_REDESIGN_REQUIRED

repair >=1/10 and measured oracle >=7/16
  REDUCED_Q0_TRANSPORT_BRIDGE_R51R4D4_CENTER_BRIDGED_TWO_PULSE_AUTHORITY_PRESENT_R51R4D5_MODEL_CONTROLLER_PREFLIGHT_REQUIRED
```

## 9. Scientific and learning boundary

D4 is a finite development-envelope sequential-response discriminator. It is
not an independent qualification set, causal selector, observer, controller,
MPC, continuous-parameter/noise/disturbance/long-hold test, Gate A result, or
global plant-reachability claim. A PASS authorizes only the already frozen
zero-new-TSC D5 preflight; a FAIL rejects only this fixed two-pulse family.

All D4 trajectories are probes forbidden from expert data, BC, DAgger,
residual RL, or other learning. Gate A and Gate B remain blocked.
