# Stage4.2R3c3T13S24D1R14R8R14 cumulative multidirection staircase authority identification design

Frozen prospectively on 2026-08-08 after final R8R13 primary/independent
agreement and forensic closure, but before R8R14 implementation,
configuration, offline construction, candidate result, formal metric, raw,
snapshot, or TSC execution.

## 1. Question and necessity

R8R12 safely measured one four-level cumulative direction-2-positive
staircase in all sixteen R8R7 contexts. It generated substantial signed-margin
movement in several failed contexts but repaired none. R8R13 then proved that
adding any one measured short direction-0 transient under an optimistic
separable R/Z/Ip construction still repaired `0/10` failures.

The existing isolated pulses, returning multipulses, R8R12 single direction,
and R8R13 offline superposition do not measure how the plant responds to
persistent cumulative motion along the other response coordinates. R8R14
therefore asks a new finite identification question:

```text
Does the exact safe cumulative four-level staircase family over all four
fixed response coordinates and both signs contain measured formal authority
for at least one currently failed restart context?
```

R8R14 is a prospective real-TSC sequence-identification campaign. It is not
a fitted controller, causal selector, receding-horizon MPC, Gate A, expert
data, or learning stage. Its retrospective oracle is evaluator-only and can
never be exposed to a controller.

## 2. Immutable sources

Use the exact accepted R8R7 source bank:

```text
run
  stage4_2r3c3t13s24d1r14r8r7_fresh_multipulse_static_observer_interaction_sentinel_20260807_d8d231e_v1
stage
  stage4_2r3c3t13s24d1r14r8r7_fresh_multipulse_static_observer_interaction_sentinel
route
  FRESH_MULTIPULSE_STATIC_OBSERVER_INTERACTION_PASS_MPC_DESIGN_REQUIRED
baseline raw
  16 files / 487298 bytes
  46df626a462dfdbfe7cdf9138a50b6b19c03f0ae5e8bb18cf18c6fcbe05c01a5
final / manifest / state SHA-256
  9ca6afce52442c1b7470cb8ff13a2eac4aab32de1e05a898d206f5fe76e01005
  ae89fb2df01cd6758676baa895880e2005a1f8967c62ea4ae671ab61ea771e24
  04f643e09f414c5d5a305f1a3584450e12e5a39e8d062d454480df3c8d95fe7e
```

Reuse, without rerunning, the exact accepted R8R12 v2 direction-2-positive
staircase:

```text
run
  stage4_2r3c3t13s24d1r14r8r12_causal_cumulative_direction2_staircase_authority_sentinel_20260808_523c706_v2
route
  CAUSAL_CUMULATIVE_DIRECTION2_STAIRCASE_AUTHORITY_INSUFFICIENT_SEQUENCE_REDESIGN_REQUIRED
safety raw
  4 files / 130839 bytes
  8eb94b448e30272191ebbf6440fdc5a104025d2d407ee914404cf5452fca4beb
qualification raw
  12 files / 395289 bytes
  b51d78d0fa511255d09a941861e54b8b2667dbac499ce354ee94d6bb76c16bb6
detailed / summary / independent / final / manifest / state SHA-256
  
```

Authenticate the final R8R13 route and hashes as design provenance, but do
not use its synthetic composed trajectories as physical measurements:

```text
route
  MEASURED_ADDITIVE_DIRECTION0_ON_DIRECTION2_AUTHORITY_INSUFFICIENT_NEW_SEQUENCE_IDENTIFICATION_REQUIRED
detailed / summary / independent / final / manifest / state SHA-256
  
```

No source byte may be modified. All source and new R8R14 trajectories are
forbidden from expert, BC, DAgger, and RL datasets.

## 3. Exact candidate family

Use the unchanged R8R12 canonical four-column response coordinate matrix,
dynamic exact-Card15 issue construction, exact stored-target refresh,
delegated causal task-step 0--9 source prefix, and four decision steps:

```text
decision task steps  [10,14,18,22]
canonical scale      1.0
directions           [0,1,2,3]
signs                [-1,+1]
level count          4
```

Direction 2/sign positive is already the immutable R8R12 source. R8R14 must
not rerun it. The seven new physical candidates are exactly:

```text
(direction, sign)
  (0,-1) (0,+1)
  (1,-1) (1,+1)
  (2,-1)
  (3,-1) (3,+1)
```

For each candidate and each of the sixteen ordered R8R7 contexts, construct
a new exact Card15 target from the current causal measured-current center at
each decision. Hold that stored target by exact causal refresh until the next
decision; hold level four through the unchanged formal endpoint. There is no
accumulated cancellation. Pair/history labels, source results, future
measurements/actions, hidden wires, and formal outcomes are forbidden
controller inputs.

This produces a final measured atlas of 128 rows:

```text
existing R8R12 direction-2-positive  16
new R8R14 seven-candidate rows       112
final measured atlas                 128
```

## 4. Offline and physical safety gates

Before a worker, raw file, or plant advance, primary and structurally
independent offline implementations must construct all 112 new specifications
from the exact R8R7 source prefixes. Every one of 448 issues and every
inter-decision/final-hold refresh must pass:

```text
exact Card15 target/readback reconstruction
finite requested/applied action and current
incremental normalized action <= 0.25
total normalized action <= 1.0
maximum current utilization <= 0.55
no action saturation or current clipping
desired/applied-current cosine >= 0.98
relative off-basis residual <= 0.10
safe stop before a failed plant advance
```

Any offline construction failure ends R8R14 with zero new TSC. The family may
not be reduced after seeing which candidate failed.

## 5. Prospective two-phase execution

Use the unchanged ordered eight pairs times `minus_first, plus_first`. The
first two ordered pairs form four safety contexts:

```text
safety         4 contexts * 7 candidates = 28 new trajectories
qualification 12 contexts * 7 candidates = 84 new trajectories
maximum total                            = 112 new trajectories
```

Safety outcomes are limited to runtime, restart/causal prefix, calibration,
Card15 action/refresh, current, saturation, finite state, forbidden input,
raw/snapshot integrity, and action-count aggregates. Formal outcomes stay
closed. Qualification is authorized only after exact primary/independent
safety agreement and byte-frozen raw authentication.

After all 112 raw files exist, exact independent qualification raw agreement
is required before any formal outcome opens. A rejected action is not applied
and no later plant step may occur. R8R14 may not resume after a scientific or
physical-safety failure under changed semantics.

## 6. Immutable formal evaluation and authority gate

Use both unchanged formal paths:

```text
slew 1.0/1.1: arrive by state 25, hold through state 35
slew 0.9:     arrive by state 27, hold through state 37
R/Z <= 0.03 m, speed <= 0.1 m/s, Ip <= 10000 A
arrival streak 3
metric-path absolute tolerance <= 1e-12
```

First reproduce exactly:

```text
R8R7 baseline formal pass                 6/16
R8R12 direction-2-positive formal pass    6/16
```

For every context, report the baseline and all eight measured cumulative
candidates. A do-nothing-safe held oracle may choose the baseline or one
candidate only for evaluator-side authority diagnosis. It is not causal
policy evidence and cannot authorize direct TSC control.

The scientific gate passes only if:

```text
all source/offline/raw/safety/formal/independent integrity gates pass
failed baseline count                                      10
at least one failed baseline is repaired by a measured candidate
held baseline-or-eight-candidate oracle formal pass      >= 7/16
all six baseline-pass contexts remain available through do-nothing fallback
primary/independent context outcomes, numerics, gate, route exact
```

Also report each fixed global candidate's `N/16` result, repairs,
regressions, selected arrivals, signed margins, and failed-context gain
distribution. No per-context choice is accepted as a deployable selector.

A PASS establishes only that the measured cumulative family contains finite
physical authority worth a separately frozen causal visible-state
selector/receding-horizon-controller design. It is not real MPC or Gate A.

## 7. Routes

```text
offline source/action construction failure before TSC
  CUMULATIVE_MULTIDIRECTION_ATLAS_PREFLIGHT_FAIL_NO_TSC

runtime/restart/causality/Card15/current/raw/safety failure
  CUMULATIVE_MULTIDIRECTION_ATLAS_EXECUTION_OR_SAFETY_FAIL_STOP

integrity passes but measured held authority repairs no failed baseline
  CUMULATIVE_MULTIDIRECTION_ATLAS_AUTHORITY_INSUFFICIENT_SEQUENCE_BASIS_REDESIGN_REQUIRED

measured held authority reaches >=7/16 and repairs >=1/10
  CUMULATIVE_MULTIDIRECTION_ATLAS_PASS_CAUSAL_SELECTOR_DESIGN_REQUIRED
```

No result authorizes expert data or learning. Gate A remains blocked until a
real causal MPC/controller subsequently passes every hard and finite
qualification axis in `CURRENT_TASK.md` Section 0.1.
