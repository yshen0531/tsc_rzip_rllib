# Stage4.2R3c3T13S24D1R14R8R30 static timing audit

## Result

R8R30 stopped during static source-schedule alignment review, before config,
implementation, bank construction, feature value, fit, tube, support, search,
package, deployment, controller action, raw, snapshot, Ray, `gotsc`, TSC, or
plant advance. Final route:

```text
EXPLICIT_FOUR_COORDINATE_FEEDBACK_PREFLIGHT_FAIL_NO_TSC
```

The explicit 4D/44D/238D representation is internally well-defined. The
failure is instead that the frozen 592-row bank includes all R8R28 g3
trajectories while the new model samples only decision origins:

```text
R8R30 model decisions  [10,12,14,16,18,22]
R8R28 g3 issues        [10,13,16,19]
```

Consequently a g3 transition from origin 12 to origin 14 contains a real new
major issue at task step 13, and its transition from origin 18 to origin 22
contains a real new major issue at task step 19. R8R30's feature encodes only
the current origin coordinate and causal past. It has no field for a future
action inside the predicted interval. Labeling those origins `q=0` would
therefore train a no-new-increment model against targets generated by an
unencoded future nonzero action.

This is a prospective source/action-timing design failure caught before any
feature or model computation. It is not runtime, package, source corruption,
raw, restart, causality, Card15, safety, model-fit, controller, real-MPC,
formal-control, plant, Gate A, or reachability evidence.

## Frozen evidence

R8R30 design checkpoint is `788da93`:

```text
docs/codex/reports/
STAGE4_2R3C3T13S24D1R14R8R30_EXPLICIT_FOUR_COORDINATE_MEASUREMENT_RECENTERED_FEEDBACK_SENTINEL_DESIGN.md
SHA-256  6a65a7ed7e84cbd89f420e2461928ce0cafcf30a96768b8836d67ccc9508d1f4
```

No R8R30 file exists beyond its design and this audit. No scientific metric
or action-authority outcome was opened.

The R8R28 g2 schedules are aligned:

```text
g2 issues             [10,12,14,16]
R8R30 decisions       [10,12,14,16,18,22]
```

After task step 16, g2 uses only exact-target refresh, so it introduces no
unencoded new coordinate within the 16--18 or 18--22 intervals. All R8R23
and selected R8R14 source issue steps are `[10,14,18,22]`, also a subset of
the proposed six-decision grid.

## Corrective boundary

R8R30 is immutable. A new identity may preserve its exact 4D/44D/238D
representation and every controller/safety/formal gate, but must exclude the
32 g3 rows and retain only the 32 aligned g2 R8R28 rows. The corrected exact
bank becomes:

```text
R8R23                                      432
six non-U/V R8R14 schedules                96
R8R28 g2 only                               32
total                                      560
global schedule identities                  35
six interval records per trajectory       3360
```

Alternatively adding task steps 13 and 19 would materially change the
controller grid and is not authorized under R8R30. The smaller aligned-bank
route is the eligible next identity.

All R8-family evidence remains forbidden from learning data. Gate A remains
blocked.
