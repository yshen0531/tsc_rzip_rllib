# Fixed-1000 D3 discrete candidate-value result

## Verdict

D3 completed all `16/16` authentic rollouts and `768/768` verified plant
advances.  The independent raw audit passed `784` states, `768` exact Card15
actions and `752` observed-slew transitions.  Both zero-fit integrity replays
were exact in R/Z/Ip, 14 coil currents, 48 wire currents and semantic
artifacts.

The final routes are:

- primary: `ONE_MS_NR1000D3_DISCRETE_VALUE_DEVELOPMENT_PASS_ONE_MODEL_ONLY`;
- independent: `ONE_MS_NR1000D3_INDEPENDENT_PASS`.

This is a development-data PASS, not Authority, Recourse, calibration,
holdout, feedback or tracking qualification.

## Load-bearing measurements

The exact cumulative candidate input span was rank three with condition
`1.40172`; the maximum adjacent issued step was `0.2 A`.

Under block4-plus history, signed h4/h8 separations (mm) were:

- even: `0.4111 / 0.6037`;
- odd: `0.6530 / 1.8294`;
- block4: `0.5621 / 1.1751`.

Under block4-minus history they were `0.4245 / 0.5945`,
`0.6486 / 1.8250`, and `0.5756 / 1.1833`.  All three axes were supported in
both histories.  The measured task-plane cone had h4 weakest-best projection
`0.1799/0.1796 mm`, h8 `0.3288/0.3366 mm`, maximum angular gap about
`90.8 degrees`, and condition below `1.98`.  Ip and h12/h16 return-tail gates
passed.

## Route decision

The G3 block2 failure is unchanged and remains a negative development label.
D3 shows that a finite unequal candidate library is viable under two new
complete causal histories; it does not restore a uniform basis claim.

Exactly one direct, support-gated candidate-value model may now be developed.
It must predict matched no-action and candidate h4/h8 outcome/value directly
from current exact R/Z/Ip and causal history, include support refusal, use
whole-history splits, and avoid recursive open-loop world-state rollout.
After development PASS, exactly one fresh calibration and one unopened blind
whole-history campaign are allowed.  Authority and Recourse remain independent
AND gates before any feedback sentinel.  A model FAIL closes this candidate
library without a third model or adjacent action-parameter ladder.

