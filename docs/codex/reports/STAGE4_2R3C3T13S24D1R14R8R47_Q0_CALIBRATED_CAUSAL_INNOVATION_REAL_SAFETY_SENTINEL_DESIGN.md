# Stage4.2R3c3T13S24D1R14R8R47 q0-calibrated causal-innovation real safety sentinel design

Status: conditionally and prospectively frozen on 2026-08-09 after the R8R46
scientific design and implementation were frozen, but before any R8R46 bank
rebuild, fit, innovation, tube, prediction, plan, metric, route, primary result,
independent result, or final result was computed or opened. No R8R47 config,
implementation, package, deployment, controller, Ray task, `gotsc`, TSC, plant
step, raw, snapshot, metric, or result exists.

## 1. Conditional source gate

R8R47 may be implemented only if final primary/independent R8R46 evidence is
exactly authenticated and its final route is:

```text
Q0_CALIBRATION_CAUSAL_INNOVATION_PREFLIGHT_PASS_REAL_SENTINEL_DESIGN_REQUIRED
```

The future R8R47 config must bind the exact final R8R46 primary summary,
primary detailed report, model, independent report, compact audit, final report,
stage state, stage manifest, config, design, implementation checkpoint, package
manifest, and package hash-list SHA-256 values. Observed hashes may be inserted
only after a PASS; no scientific threshold or action semantic may change then.

Any R8R46 non-PASS permanently blocks this identity. R8R47 may not be
repurposed as another offline preflight, an R8R46 rerun, or a relaxed model.

## 2. Exact real-controller identity

Use one fresh authentic restart rollout for each of the immutable 16
pair/history contexts. The finite matrix, ordering, targets, delay, slew,
restart snapshots, source hashes, and context identities are inherited without
selection. There are exactly 16 tasks and no development rerun, enlargement,
replacement, or outcome-conditioned retry under this identity.

At authentic task step 10 the controller issues only candidate `q0`: an exact
Card15 current-target hold. It observes the completed interval through task
step 12 before any transport decision. Thereafter the only decision steps are:

```text
[12, 14, 16, 18, 22]
```

At each decision it uses only completed visible R/Z/Ip samples, backward
visible velocity, issued/read-back coil current, prior coordinate/action, and
its own reset causal innovation state. Pair/history labels, evaluator outcomes,
future measurements, hidden simulator state, wire/vessel current, and another
rollout are forbidden controller inputs.

## 3. Frozen model, innovation, and receding execution

Authenticate and load the exact R8R46 model artifact. Preserve the fixed cold
ensemble, features, normalizations, neighbor ordering, ties, solver results,
weights, tube, support hulls, 17 candidates, beam width 512, costs, formal
evaluator, and dynamic exact Card15 radius 16.

The per-rollout innovation is reset to zero. After each completed interval use
the exact R8R46 update:

```text
b_(k+1) = clip(0.5*b_k + 0.5*r_k, -B_k, +B_k)
```

`r_k` is computed only against the prediction actually issued before that
interval. `B_k` is the frozen training-only pre-reserve cold tube cap for that
interval. Add the full current bias to the immediate next prediction and
`0.5^h*b` to hypothetical later intervals. Future unobserved innovations are
zero. After every completed measurement discard the old suffix, update once,
and replan from the measured state/current. Never execute a stale second action.

Execute only the first nonzero action of a supported robust-formal plan. A best
failing plan is diagnostic and forbidden. Otherwise issue exact current-target
hold if that hold is safe; if it is not safe, stop before the next plant
advance.

## 4. Two-certificate safety and immutable formal timing

Every attempted issue and refresh must pass exact Card15, finite-value,
incremental action `<=0.25`, total action `<=1.0`, current utilization
`<=0.55`, desired/applied cosine `>=0.98`, off-basis residual `<=0.10`, state
support, transition support, and no clipping/saturation. Failed gates stop
before applying the action or advancing the plant.

For the immediately executed interval, use the componentwise maximum of the
unshrunk R8R43 cold tube and the final R8R46 adapted tube. Use the adapted tube
for the remaining hypothetical suffix. Innovation may not shrink the immediate
safety reserve.

The formal contract remains:

```text
slew 1.0/1.1   arrive by 250 ms; hold/evaluate through 350 ms
slew 0.9       arrive by 270 ms; hold/evaluate through 370 ms
R/Z            0.03 m
speed          0.1 m/s
Ip             unchanged 10000 A
arrival streak unchanged 3 samples
```

No later deadline, partial trajectory, probe metric, or fallback-only result
may be counted as a formal pass.

## 5. Prospective real-sentinel gate

Primary raw postprocessing and a structurally independent raw/snapshot audit
must establish:

```text
exact source/package/config/controller authentication                 PASS
authentic restart / causal prefix / task completion                 16/16
q0 exact hold and completed calibration measurement                 16/16
finite model / support / innovation / dual-certificate safety       16/16
Card15 issue and refresh / current / action / forbidden-input gates  PASS
safe controller search or fail-closed hold at every decision         PASS
runtime / solver / saturation / raw / snapshot errors                    0
formal regressions among the six frozen baseline passes                0/6
formal repairs among the ten frozen baseline failures                 >=1/10
fallback-plus-controller formal oracle                                >=7/16
contexts executing a safe nonzero first transport action                  >=1
primary/independent raw, trace, metric, selection, and route agreement exact
```

The route is a design/safety failure if all executions are clean but the
authority requirements fail. Runtime/deployment/import errors, safe structured
stops, reporting errors, real closed-loop design failures, and formal control
failures must remain separately classified.

## 6. Routes and limits

```text
source or package blocked
  Q0_CALIBRATED_REAL_SENTINEL_BLOCKED_BY_SOURCE

runtime, deployment, import, raw, or snapshot failure
  Q0_CALIBRATED_REAL_SENTINEL_EXECUTION_FAIL_STOP

unsafe attempted action, unsupported state/transition, clipping, or failed
fail-closed behavior
  Q0_CALIBRATED_REAL_SENTINEL_SAFETY_FAIL_STOP

clean safe execution but frozen real authority gate fails
  Q0_CALIBRATED_REAL_SENTINEL_AUTHORITY_INSUFFICIENT_REDESIGN

all execution, safety, authority, formal, and dual-audit gates pass
  Q0_CALIBRATED_REAL_SENTINEL_PASS_QUALIFICATION_DESIGN_REQUIRED
```

A PASS is only a finite clean same-source 16-context real-controller safety
sentinel. It is not continuous-parameter, noise, disturbance, long-hold,
unseen-target, hidden-history, or restart-envelope qualification; it is not
Gate A and does not authorize expert data, BC, DAgger, residual RL, or Gate B.
The next qualification design must be frozen separately under the 2026-08-06
finite qualification policy.

All R8R47 trajectories are controller-development evidence and are forbidden
from every expert or learning dataset, regardless of outcome.
